function setup() {
    createCanvas(600,600);
      background("purple");
  }
  
  function draw() {
  
    stroke("lightgreen");
  fill("darkred");
    
      // console.log(mouseIsPressed);
  
  if(mouseIsPressed){
    rect(mouseX, mouseY, 10,-100
        );
  }
  }